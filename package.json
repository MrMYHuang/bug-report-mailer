{
  "name": "bug-report-mailer",
  "version": "1.1.1",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && tsc",
    "start": "node index.js",
    "pack": "rm -f a.zip && zip -r a.zip package.json index.js dist node_modules",
    "deploy": "npm run pack && ./scripts/deploy-lambda.sh"
  },
  "description": "",
  "author": {
    "name": "Meng-Yuan Haung"
  },
  "dependencies": {
    "nodemailer": "^6.6.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.81",
    "@types/nodemailer": "^6.4.4",
    "typescript": "^4.3.4"
  }
}
